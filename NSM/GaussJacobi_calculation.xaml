<Page
    x:Class="NSM.GaussJacobi_calculation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NSM"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

   
        <Grid x:Name="LayoutRoot" Margin="0,-26.667,0,-0.333">
            <Grid.RowDefinitions>
                <RowDefinition Height="100" />
                <RowDefinition Height="50" />
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Name="motu" Text="Gauss Jacobi Method" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Bottom" />
            <TextBlock Text="Solution :" Grid.Row="1" FontSize="25" VerticalAlignment="Bottom" />
        <Line Stroke="White"  Name="lines" VerticalAlignment="Bottom" StrokeThickness="3" X1="0" X2="500" />

        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <StackPanel Grid.Row="2" Name="StackPanalPrint" />
        </ScrollViewer>
            
    </Grid>
 </Page>
